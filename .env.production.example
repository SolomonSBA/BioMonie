# Production build (optional).
# Leave unset for SmarterASP single-folder deploy (API + site on same origin → POST /api/contact).
# Set only if the API runs on a different domain:
# VITE_CONTACT_API_URL=https://your-biomonie-domain.com
